<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================ -->
    <!-- SEGMENTO DARK THEME - Monochrome Premium Palette              -->
    <!-- ============================================================ -->

    <!-- Background layers - depth via subtle gradations -->
    <Color x:Key="WindowBackgroundColor">#0F0F10</Color>
    <Color x:Key="SidebarBackgroundColor">#161617</Color>
    <Color x:Key="SurfaceColor">#1C1C1E</Color>
    <Color x:Key="SurfaceElevatedColor">#232325</Color>
    <Color x:Key="SurfaceHoverColor">#2A2A2D</Color>
    <Color x:Key="SurfaceActiveColor">#323236</Color>

    <!-- Borders -->
    <Color x:Key="BorderColor">#2A2A2D</Color>
    <Color x:Key="BorderSubtleColor">#1F1F22</Color>
    <Color x:Key="BorderEmphasizedColor">#3F3F44</Color>

    <!-- Text -->
    <Color x:Key="TextPrimaryColor">#FAFAFA</Color>
    <Color x:Key="TextSecondaryColor">#A1A1A6</Color>
    <Color x:Key="TextTertiaryColor">#6E6E73</Color>
    <Color x:Key="TextDisabledColor">#48484A</Color>

    <!-- Accent (monochrome only - white) -->
    <Color x:Key="AccentColor">#FFFFFF</Color>
    <Color x:Key="AccentHoverColor">#E5E5E5</Color>
    <Color x:Key="AccentPressedColor">#CCCCCC</Color>

    <!-- Inverse (for white buttons) -->
    <Color x:Key="OnAccentColor">#0F0F10</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}"/>
    <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="{StaticResource SidebarBackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceElevatedBrush" Color="{StaticResource SurfaceElevatedColor}"/>
    <SolidColorBrush x:Key="SurfaceHoverBrush" Color="{StaticResource SurfaceHoverColor}"/>
    <SolidColorBrush x:Key="SurfaceActiveBrush" Color="{StaticResource SurfaceActiveColor}"/>

    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource BorderSubtleColor}"/>
    <SolidColorBrush x:Key="BorderEmphasizedBrush" Color="{StaticResource BorderEmphasizedColor}"/>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiaryColor}"/>
    <SolidColorBrush x:Key="TextDisabledBrush" Color="{StaticResource TextDisabledColor}"/>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
    <SolidColorBrush x:Key="AccentPressedBrush" Color="{StaticResource AccentPressedColor}"/>
    <SolidColorBrush x:Key="OnAccentBrush" Color="{StaticResource OnAccentColor}"/>

    <!-- Danger (minimal) -->
    <SolidColorBrush x:Key="DangerBrush" Color="#EF4444"/>
    <SolidColorBrush x:Key="DangerHoverBrush" Color="#DC2626"/>

    <!-- Overlays -->
    <SolidColorBrush x:Key="OverlayBrush" Color="#99000000"/>

</ResourceDictionary>
